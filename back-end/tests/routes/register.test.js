const axiosist = require('axiosist');
const app = require('../../api/server');
const fixtures = require('../fixtures/register');

describe('POST /register', () => {
  
  describe('when name is less than 12 characters', () => {
    
  });
});
